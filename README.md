

HTTP             || ROUTE             || Action            || Used For
get                 '/'                  index                user signup

get                 '/users/login'       show                 user login             
